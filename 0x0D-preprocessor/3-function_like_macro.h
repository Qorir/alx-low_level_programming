#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * Desc: header file that define a function-like a macro ABS(x) thatcompute the absolute valueof a number x
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
