#include "main.h"

/**
 * _abs - computes the absolute value of integer
 * @n: is the integer to be computed
 * Return: absolute value of a character
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
