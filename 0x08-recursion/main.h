#ifndef MAIN_H
#define MAIN_H

/**
 * header filr containing all the prototypes used in 0x08-recursions
 */

int _putchar(char c);
void _puts_recursion(char *s);
void _print_rev_recursion(char *s);
int _strlen_recursion(char *s);

#endif
